# Smithery-Compliant MCP Job-Scraper Server – Python package requirements
# ----------------------------------------------------------------------
# Pinned to minimum versions known to work at the time of development. A
# production deployment may wish to pin *exact* versions or use a lock
# file for full reproducibility.

# Core HTTP client
requests>=2.31.0

# HTML parsing
beautifulsoup4>=4.12.2
lxml>=4.9.3

# Optional: environment-variable overrides (recommended for Docker / cloud)
python-dotenv>=1.0.0

# Development / testing extras – not strictly required at runtime
# fake-useragent provides a much larger User-Agent pool than the small
# static list shipped in config.py. It is safe to omit in constrained
# environments.
fake-useragent>=1.5.0  # optional
pytest>=7.4.0          # optional
