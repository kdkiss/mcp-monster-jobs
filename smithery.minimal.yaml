name: mcp-minimal-test
displayName: Minimal MCP Test
version: "1.0.0"
description: Minimal test configuration for Smithery.ai

build:
  dockerfile: Dockerfile.minimal
  context: .
  timeout: 300

runtime:
  command: node
  args: ["-e", "console.log('Minimal MCP server running')"]
  env: {}
  ports: []