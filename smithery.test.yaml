name: test-mcp-server
displayName: Test MCP Server
version: "1.0.0"
description: Minimal MCP server for testing Smithery.ai

build:
  dockerfile: Dockerfile.test
  context: .
  timeout: 300

runtime:
  command: node
  args: ["test_mcp_server.js"]
  env: {}
  ports: []